import xmltodict
import pprint
import json

def fun():
#   with open('Validation.xml') as fd:
#       doc = xmltodict.parse(fd.read())
#       prod = doc['product']
#       urls = prod['urls']
#       url = urls['url']
#       s = json.dumps(url)
   return 's'
fun()
